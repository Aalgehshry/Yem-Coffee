node_modules
dist
dist-ssr
*.local
.vercel
.DS_Store

# Claude Code local tooling (dev-server launch config, personal settings).
# If shared project settings are ever wanted in the repo, add them explicitly
# with `git add -f .claude/settings.json`.
.claude/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
